name: CI
permissions:
  contents: read
  pull-requests: read

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - 'api/**'
      - 'src/**'
      - 'tests/**'
      - 'benchmarks/**'
      - 'CMakeLists.txt'
      - 'conanfile.py'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CONAN_HOME: ${{ github.workspace }}/.conan2
      INSIGHT_CANON_REPO: coderoast-dev/insight-canon
      CR_PIN_INSIGHT_CANON: '1.2.8'

    steps:
      - uses: actions/checkout@v4

      - name: Setup environment
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build python3-pip build-essential clang-tidy

      - name: Setup Conan
        uses: conan-io/setup-conan@v1
        with:
          home: ${{ env.CONAN_HOME }}
          cache_packages: true
          profile_detect: false

      - name: Vendor insight_canon from insight-canon release
        env:
          GH_TOKEN: ${{ secrets.INSIGHT_CANON_RELEASE_TOKEN }}
        run: |
          bash scripts/ci_fetch_conan_package.sh insight_canon "$CR_PIN_INSIGHT_CANON" "$INSIGHT_CANON_REPO"

      - name: Build and test
        run: |
          conan create . \
            --profile:host="$CONAN_HOME/profiles/linux-gcc13-release" \
            --profile:build="$CONAN_HOME/profiles/linux-gcc13-release" \
            --build=missing \
            --build-test=missing
