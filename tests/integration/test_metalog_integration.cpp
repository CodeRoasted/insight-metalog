// NOLINTBEGIN Placeholder for metalog-vs-LogCraft format compatibility
// tests. Skipped by default — real assertions land in follow-up
// commits as the metalog chain is exercised against deterministic
// LogCraft scenario outputs.
#include <gtest/gtest.h>

TEST(InsightMetalogLogCraftCompat, Placeholder)
{
    GTEST_SKIP() << "Format-compatibility tests against logcraft_core "
                    "land in a future commit; this file exists to anchor "
                    "the per-phase integration directory + CMake gate.";
}
// NOLINTEND
